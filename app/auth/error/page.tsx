import React from 'react'
import AuthErrorContent from './AuthErrorContent'

export default function AuthErrorPage() {
  // No client hooks here, just return the client component
  return <AuthErrorContent />
}